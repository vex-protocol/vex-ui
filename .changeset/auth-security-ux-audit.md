---
---

Separate account creation, password sign-in, passkey sign-in, passkey-backed password recovery, password changes, and new-device enrollment throughout the clients. Add complete passkey and approved-device management on desktop and mobile, make sign-out durable, store database keys independently in platform secure storage, preserve drafts and attachments when sends fail, and improve account-picker, composer, modal, settings, and message-list usability. Split desktop routes and heavy protocol/crypto dependencies into deferred chunks so the initial application bundle stays focused on startup.
